<input class="field__input" name="{{ name }}" id="{{ id }}" type="{{ type }}" {{#if value}} value="{{ value }}"{{/if}} {{#if placeholder}} placeholder="{{ placeholder }}"{{/if}} {{#if accept}} accept="{{ accept }}"{{/if}} />
